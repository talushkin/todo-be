// Registration page: renders the RegisterForm for new user sign-up.
// Simple, focused, and uses a modular component for form logic.
// 2025-09-08
import React from 'react';
import RegisterForm from '../components/RegisterForm';

const RegisterPage: React.FC = () => {
  return <RegisterForm />;
};

export default RegisterPage;
